<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aide.ui.re" >

    <!-- 修改或删除您的SD卡中的内容 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <!-- 读取您共享存储空间中的内容 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
        <!-- 所有文件访问权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        android:resizeableActivity = "true">
        <!--安卓11开始必须添加 所有文件访问权限 android:requestLegacyExternalStorage="true"-->
        <!--使用 android:theme="@style/AppTheme.Translucent" 可隐藏默认Toolbar导航栏-->
		
        <activity
            android:exported="true"
            android:name="com.aide.ui.re.MainActivity"
            android:label="@string/app_name" 
			android:configChanges="orientation|keyboardHidden|screenSize|uiMode|keyboard|navigation"
			android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
